@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>

@{
    ViewBag.Title = "Список ролей";
    ViewBag.Secondary = "Для администратора";
}
 <h1 class="mb-5">@ViewBag.Title 👮</h1>
<table class="table">
   
    @foreach (var role in Model)
    {
        <tr>
            <td>@role.Name</td>
            <td>
                <form asp-action="Delete" asp-route-id="@role.Id" method="post">
                    <button type="submit" class="btn btn-sm btn-danger">
                        Удалить
                    </button>
                </form>
            </td>
        </tr>
    }
</table>
<a class="me-2"asp-action="Create">Добавить роль</a>
<a asp-controller="Role" asp-action="UserList">Список пользователей</a>